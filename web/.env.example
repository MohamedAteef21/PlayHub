# Base URL of the backend API (no trailing slash).
# Leave empty in local dev — the Vite dev server proxies /api, /hubs and /uploads to localhost:5052.
# In production (Vercel) set this to your MonsterASP site URL, e.g.:
# VITE_API_URL=https://playhub.runasp.net
VITE_API_URL=
